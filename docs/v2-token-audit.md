# v2 token audit (P1.4)

**Inventory only, per `docs/v2-spec.md`'s P1.4 section: "Convert nothing
here. Conversion is P2, in its own sessions."** Every count below was
produced by direct, reproducible commands against the real files in this
repo (not estimated), and spot-checked line-by-line before being recorded
here. Re-run the commands in each section's own note to regenerate a count
if the file changes before P2 starts.

## Scope and structure

This codebase has **no per-component directory structure** — no
`public/components/`, no `public/css/`. The real layout is:
- `public/styles.css` — one 2,354-line stylesheet.
- `public/moonlit-shrine-themes.css` — the actual 53 per-theme colour
  definitions (generated by `scripts/generate-themes.js`), imported by
  `styles.css`. **Out of scope for this inventory** — that file *is* the
  intentional colour source of truth, not stray literals to clean up.
- `public/js/*.js` — 25 files, flat, one directory.

So "grouped by directory" below means, in practice: `public/styles.css`
grouped by its own `/* ---------- Section ---------- */` comment blocks
(the file's real substructure), and `public/js/` grouped by individual
file.

## `public/styles.css`: hardcoded `font-size: Npx` — 75 occurrences

Command: `grep -oE "font-size\s*:\s*[0-9.]+px" public/styles.css | wc -l`

| Section | Line range | Count |
| --- | --- | --- |
| Series card, six states | 773–1513 | 25 |
| Schedule page | 1675–1805 | 10 |
| Help panel | 1946–2100 | 8 |
| Import steps + confidence review | 2100–2149 | 7 |
| Home dashboard | 1513–1569 | 6 |
| Filter bar | 584–761 | 5 |
| Header | 238–402 | 2 |
| Main / toolbar | 555–584 | 2 |
| Home | 1633–1662 | 2 |
| Anime detail overlay | 2149–2268 | 2 |
| Atmosphere | 130–238 | 1 |
| Banners | 402–431 | 1 |
| Empty states | 1805–1825 | 1 |
| Overlays | 1825–1904 | 1 |
| Confirm dialog | 1904–1928 | 1 |
| Toasts | 2268–2309 | 1 |
| **Total** | | **75** |

No single selector repeats this pattern heavily — the 75 hits spread across
~70 distinct one-off selectors (`.stat-value`, `.card-title`,
`.discover-age`, `.schedule-day-name`, `.faq summary`, `.detail-native`,
...), each declaring its own literal size rather than reusing a shared
class.

## `public/styles.css`: hardcoded spacing (`margin`/`padding`/`gap` + px) — 275 occurrences

Command:
`grep -oE "(margin(-top|-bottom|-left|-right)?|padding(-top|-bottom|-left|-right)?|gap|row-gap|column-gap)\s*:\s*[^;}]*" public/styles.css | grep -E "[0-9]px" | wc -l`

| Section | Line range | Count |
| --- | --- | --- |
| Series card, six states | 773–1513 | 52 |
| Help panel | 1946–2100 | 49 |
| Anime detail overlay | 2149–2268 | 28 |
| Filter bar | 584–761 | 22 |
| Schedule page | 1675–1805 | 20 |
| Import steps + confidence review | 2100–2149 | 19 |
| Settings panel | 1928–1946 | 12 |
| Header | 238–402 | 9 |
| Hero | 1569–1633 | 9 |
| Home dashboard | 1513–1569 | 9 |
| Confirm dialog | 1904–1928 | 6 |
| Home | 1633–1662 | 6 |
| Main / toolbar | 555–584 | 6 |
| Mobile nav menu (hamburger) | 2313–end | 7 |
| Overlays | 1825–1904 | 5 |
| Toasts | 2268–2309 | 4 |
| Banners | 402–431 | 3 |
| Empty states | 1805–1825 | 3 |
| Buttons, four levels | 478–555 | 2 |
| Atmosphere | 130–238 | 1 |
| Save indicator | 431–453 | 1 |
| Card grid | 761–773 | 1 |
| Statistics page | 1662–1669 | 1 |
| **Total** | | **275** |

By property: `gap` is the largest single contributor, followed by
`padding` and `margin-bottom`; a handful of selectors declare several
spacing values in one rule (`.app-header`, `.bulkbar`, `.detail-meta-grid`,
`.card-grid`, `.discover-card`, `.season-row`, the `.schedule-*` family),
but most of the 275 are broadly distributed one-off literals, not a small
set of hot spots — P2 will touch most of the stylesheet's rules, not a
concentrated handful.

## `public/styles.css`: hardcoded colour literals outside the token block — 4 occurrences

Command:
`grep -n "rgba(\|rgb(\|#[0-9a-fA-F]\{3,8\}" public/styles.css` (excluding the
`:where(:root)`/`[data-text-size]`/`[data-text-weight]` token-definition
block at lines 23–106, which is the intentional source of truth, not a
stray literal).

All 4 are `rgba()` overlay/scrim darkening, all in the Series card section:

| Line | Declaration |
| --- | --- |
| 1115 | `background: rgba(0,0,0,0.65);` |
| 1215 | `background: linear-gradient(75deg, transparent 30%, rgba(255, 255, 255, 0.4) 50%, transparent 70%);` |
| 1382 | `background: rgba(0, 0, 0, 0.55);` |
| 1391 | `.corner-btn:hover { background: rgba(0, 0, 0, 0.85); }` |

Zero hex codes and zero raw `hsl()`/`hsla()` literals exist outside the
token block — every other colour reference in component rules already goes
through `var(--...)` or `color-mix(in srgb, var(--...) X%, transparent)`.
Colour-literal cleanup in `styles.css` itself is a very small, contained
item compared to font-size and spacing.

## `public/js/`: inline hardcoded literals

Checked every `.style.X =` / `.style.setProperty(...)` call, every inline
`style="..."` template-string attribute, and every canvas `ctx.font`/
`ctx.fillStyle` call across all 25 files.

**Not relevant (data-driven, not a hardcoded design literal) — excluded:**
- `atmosphere.js:66-91` — `--leaf-*`/`--f-*` custom properties set from
  `rand(min,max)` (animation randomization, not fixed values).
- `events.js:185,188-189` — tab-pill slider position from measured DOM
  geometry (`offsetLeft`/`offsetWidth`).
- `events.js:1561-1563` — ripple effect size/position from click
  coordinates and measured element size.
- `render.js` progress-bar/stagger-delay/background-image inline styles —
  all computed from data (`dataset.targetWidth`, `staggerDelayMs(index)`,
  an image URL), not literals.
- `render.js:506`, `screenshotImport.js:90`, `malImport.js:128` —
  `style="color:var(--faint)"` — already tokenized.
- `statsExport.js`'s `fillStyle`/`strokeStyle` calls — already read live CSS
  custom properties via `getComputedStyle` at draw time (`readColors()`,
  lines 19–34), not hardcoded colours.

**Relevant hardcoded literals found:**

*Inline spacing in `render.js` template strings — 8 occurrences* (command:
`grep -noE 'style="[^"]*(margin|padding|gap)[^"]*"' public/js/*.js | grep -E "[0-9]px"`):

| Line | Declaration |
| --- | --- |
| 176 | `style="align-self:flex-start;padding:6px 12px;"` |
| 680 | `style="margin-top:20px"` |
| 681 | `style="gap:22px"` |
| 1144 | `style="margin-top:14px"` |
| 1552 | `style="margin-top:8px"` |
| 1553 | `style="margin:10px 0 0;font:var(--t-meta);color:var(--faint)"` (margin literal; font/colour already tokenized) |
| 1607 | `style="margin-top:8px"` |
| 1682 | `style="margin-top:18px"` |

*Hardcoded canvas font sizes in `public/js/statsExport.js` — 9 occurrences*
(command: `grep -n "ctx.font\s*=" public/js/statsExport.js`), lines 70
(40px), 74 (13px), 105 (14px), 109 (44px), 132 (13px), 136 (16px), 156
(13px), 159 (20px), 165 (13px). This file's colours are already
token-driven (see above); only the font-size numbers, plus scattered canvas
layout constants (`CARD_WIDTH=800`, `tileW=336`, `tileH=130`, `gap=24`,
`gridTop=160`, corner-radii `16`/`20`, text x/y coordinates), are hardcoded.
Whether pixel dimensions for an *exported PNG* count as "spacing" for token
purposes is a P2 scoping question, not answered here.

## A second colour source outside "components," flagged for P2's awareness

`public/js/themes.js:10-64` — a flat data array of the app's ~54 colour
themes, each with `accent1`/`accent2` hex literals (100+ hex codes total),
used only to render theme-picker swatch preview dots (the actual per-theme
CSS custom properties live in `public/moonlit-shrine-themes.css`, already
out of scope above). This is a data/config registry, not a stray literal in
a component — flagged here so P2 explicitly decides whether to account for
it or exclude it the same way the CSS theme file is excluded, rather than
it being silently forgotten.

## P2 conversion progress (resume state)

Per docs/v2-spec.md's P2 section: "A session ending mid-sweep is normal. The
audit file's done markers plus `git log --all` are the resume state." This
table is that resume state — check it, plus
`git log --all --oneline --grep "^v2(P2)"`, before continuing.

**Token-target decision** (see the `v2(P2)` commit that added the baseline
test for the full reasoning): convert to the OLD, already-live token system
(`--fs-*`/`--sp-*`/`--t-*`, driven by the existing Text Size setting), never
the new dormant P1.4 system (`--font-scale` etc.) — that stays P3.2's job to
wire up.

**Mechanical rule**: a literal converts ONLY when it EXACTLY matches an
existing `--fs-*` or `--sp-*` value. A near-miss (e.g. 10px vs. `--fs-micro`'s
10.5px) is never snapped — that would be a real, if tiny, visual change, which
the spec forbids ("does not license touching logic. Values only"). Non-matches
are left alone and reported below with why.

`--fs-*` base values (before `--text-scale` multiplies): display-l 30,
display-m 21, display-s 19, body 13, action 12.5, card 12, meta 11, micro
10.5, nano 9.5. `--sp-*` values: sp-1 4, sp-2 8, sp-3 12, sp-4 16, sp-6 24,
sp-8 32, sp-12 48, sp-16 64.

One directory (styles.css section, or one `public/js/` file) per commit, per
the spec. Baseline check (`npx playwright test
tests/e2e/token-conversion-baseline.spec.js`) plus the full unit+e2e suites
run after every single section, before its commit.

### `public/styles.css`, by section (in file order)

| Section | Status | Font-size converted | Spacing converted | Notes |
| --- | --- | --- | --- | --- |
| Moonlit Shrine tokens (the `:root` block itself) | out of scope | — | — | this IS the token source; nothing to convert |
| Atmosphere | **done** | 1 of 1 (`kbd` 11px → `--fs-meta`) | 0 of 1 | `kbd`'s `padding: 1px 6px` matches no `--sp-*` value; left alone |
| Header | not started | 0 of 2 | 0 of 9 | |
| Banners | not started | 0 of 1 | 0 of 3 | |
| Save indicator | not started | 0 of 0 | 0 of 1 | |
| Icon buttons | not started | — | — | not separately inventoried by the audit's grep pass |
| Buttons, four levels | not started | 0 of 0 | 0 of 2 | |
| Main / toolbar | not started | 0 of 2 | 0 of 6 | |
| Filter bar | not started | 0 of 5 | 0 of 22 | |
| Card grid | not started | 0 of 0 | 0 of 1 | |
| Series card, six states | not started | 0 of 25 | 0 of 52 | largest section |
| Home dashboard | not started | 0 of 6 | 0 of 9 | |
| Hero | not started | 0 of 0 | 0 of 9 | |
| Home | not started | 0 of 2 | 0 of 6 | |
| Statistics page | not started | 0 of 0 | 0 of 1 | |
| Discover page | not started | — | — | not separately inventoried by the audit's grep pass |
| Schedule page | not started | 0 of 10 | 0 of 20 | |
| Empty states | not started | 0 of 1 | 0 of 3 | |
| Overlays | not started | 0 of 1 | 0 of 5 | |
| Confirm dialog | not started | 0 of 1 | 0 of 6 | |
| Settings panel | not started | 0 of 0 | 0 of 12 | |
| Help panel | not started | 0 of 8 | 0 of 49 | |
| Import steps + confidence review | not started | 0 of 7 | 0 of 19 | |
| Anime detail overlay | not started | 0 of 2 | 0 of 28 | |
| Toasts | not started | 0 of 1 | 0 of 4 | |
| Mobile nav menu (hamburger) | not started | — | 0 of 7 | |
| The 4 raw colour literals (all `rgba()` overlays in Series card) | not started | — | — | P2 must decide separately whether an `rgba()` scrim has any equivalent token to convert to, or is left alone — not a font-size/spacing question |

Section boundaries drift slightly from the line ranges recorded earlier in
this file — P1.6/P1.7 added CSS after this audit was written. Re-run
`grep -n "^/\* ----------" public/styles.css` to get current boundaries
before starting a section; trust that over the numbers above.

### `public/js/`, by file

| File | Status | Notes |
| --- | --- | --- |
| `render.js` (inline spacing in template strings) | not started | 8 occurrences |
| `statsExport.js` (canvas `ctx.font` sizes) | not started | 9 occurrences; scoping question flagged in the audit above still open — whether an exported-PNG's pixel dimensions count as "spacing" for token purposes at all |
| every other `public/js/*.js` file | out of scope | zero inventoried literals |

## Totals

| Category | Count |
| --- | --- |
| `styles.css` hardcoded `font-size` | 75 |
| `styles.css` hardcoded spacing (`margin`/`padding`/`gap`) | 275 |
| `styles.css` hardcoded colour literals (outside the token block) | 4 |
| `public/js/` inline hardcoded spacing | 8 |
| `public/js/` hardcoded canvas font sizes | 9 |
| **Total literals inventoried** | **371** |

Not counted in the total, flagged separately for P2's scoping decision:
`statsExport.js`'s canvas layout constants (widths/gaps/radii/coordinates
for the exported PNG), and `themes.js`'s ~54-entry swatch-preview hex array.
