{
  "name": "anime-tracker",
  "version": "2.3.0",
  "private": true,
  "description": "Local-only anime tracker. No runtime dependencies, no build step. Playwright and fontkit are devDependencies only, for the v2 verification harness and the font-manifest generator script respectively.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node tests/run-all.js",
    "test:e2e:install": "playwright install chromium",
    "test:e2e": "playwright test",
    "perf": "node scripts/perf.js",
    "check:copy": "node scripts/check-copy-registry.js",
    "generate:font-manifest": "node scripts/generate-font-manifest.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "fontkit": "^2.0.4"
  }
}
